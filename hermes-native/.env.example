# Hermes Native — Environment Configuration
# Copy this file to ~/.hermes/.env and fill in values as needed.
# This file is committed to git — do NOT add real secrets here.

# LiteLLM master key (used by provider: custom to authenticate with the local stack)
OPENAI_API_KEY=hermes-local-dev
